<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Quên mật khẩu - Khách sạn Phenikaa</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background: #f8f9fc;
      font-family: 'Segoe UI', sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    main {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 3rem 0;
    }

    .auth-box {
      background: #fff;
      padding: 2rem 2.5rem;
      border-radius: 14px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      width: 380px;
      transition: all 0.3s ease;
    }

    h2 { color: #2f3e9e; margin-bottom: 25px; font-weight: 600; }

    input {
      width: 100%;
      padding: 12px 14px;
      margin-bottom: 18px;
      border: 1px solid #d0d4e4;
      border-radius: 8px;
      font-size: 15px;
      transition: all 0.2s ease;
    }

    input:focus {
      outline: none;
      border-color: #4e54c8;
      box-shadow: 0 0 0 2px rgba(78, 84, 200, 0.15);
    }

    button {
      width: 100%;
      padding: 12px;
      background: #4e54c8;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 15px;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.25s ease;
    }

    button:hover { background: #5c63da; transform: translateY(-1px); }
    a { text-decoration: none; color: #4e54c8; font-size: 14px; }
    a:hover { text-decoration: underline; }
    p.message { margin-top: 10px; font-size: 14px; }
    .success { color: #2ecc71; }
    .error { color: #e74c3c; }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <main>
    <div class="auth-box">
      <h2>Quên mật khẩu</h2>

      <% if (message) { %>
        <p class="<%= message.includes('✅') ? 'success' : 'error' %>"><%= message %></p>
      <% } %>

      <% if (step === 1) { %>
        <form action="/auth/forgot-password" method="POST">
          <input type="email" name="email" placeholder="Nhập email đã đăng ký" required />
          <button type="submit">Gửi mã OTP</button>
        </form>

      <% } else if (step === 2) { %>
        <form action="/auth/forgot-password/reset" method="POST">
          <input type="hidden" name="email" value="<%= email %>" />
          <input type="text" name="otp" placeholder="Nhập mã OTP" required />
          <input type="password" name="newPassword" placeholder="Nhập mật khẩu mới" required />
          <button type="submit">Đặt lại mật khẩu</button>
        </form>

      <% } else if (step === 3) { %>
        <p>✅ Mật khẩu của bạn đã được thay đổi thành công.</p>
        <p><a href="/auth/login">→ Quay lại đăng nhập</a></p>
      <% } %>

      <% if (step === 1) { %>
        <p><a href="/auth/login">← Quay lại đăng nhập</a></p>
      <% } %>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
